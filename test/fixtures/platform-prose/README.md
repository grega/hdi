# static-site

A static site deployed automatically.

## Install

```bash
npm install
```

## Deploy

This site is deployed on GitHub Pages via the GitHub Actions workflow.
See `.github/workflows/pages.yml` for details.
