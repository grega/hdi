# multi-deploy

An app with multiple deploy targets.

## Install

```bash
npm install
```

## Deploy

Deploy the frontend to Vercel:

```bash
npx vercel --prod
```

Deploy the API to Netlify:

```bash
netlify deploy --prod
```
